node: divergence-test-route-naming
status: complete
commits: 74daf018
changed_paths: tests/test_equilibrium_stencil_mesh.py
tests: `JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider -m "slow or not slow" tests/test_equilibrium_stencil_mesh.py` — 19 passed in 47.72s; `UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync ruff check tests/test_equilibrium_stencil_mesh.py` — passed; `UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync ruff format --check tests/test_equilibrium_stencil_mesh.py` — already formatted
test_logs: /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T084534734664-divergence-test-route-naming/logs/test-equilibrium-stencil-mesh.log
artifacts: tests/test_equilibrium_stencil_mesh.py — renamed `test_the_divergence_floor_is_truncation_and_falls_with_the_mesh` to `test_stencil_mesh_divergence_floor_is_truncation_and_falls_under_refinement`; one identifier line changed, zero assertion/tolerance/refinement-ratio changes, and the fresh-process count remained 19 passed
evidence_inputs: The renamed test explicitly identifies the non-commuting StencilMesh route whose divergence_b and divergence_j floors fall under refinement; the commuting FluxLattice assertions remain separately expressed through the `raster` ledger and retain both below-1.0e-12 checks. No other test name in the file claimed a route-dependent mechanism without identifying its route. Repository grep found eight historical/live references to the old name across six fenced-out files: docs/plans/roundoff-scale-acceptance-bounds.html (three), docs/evidence/archive/roundoff-scale-acceptance-bounds-landed.html (one), docs/figures/roundoff-scale-acceptance-bounds/acceptance-lane-verify.md (one), docs/state/nova/manifests/acceptance-lane-verify.md (one), docs/state/nova/manifests/divergence-floor-attribution.md (one), and docs/state/nova/crew.json (one). The worker did not mutate plan/index/evidence state because its exclusive implementation scope was tests/test_equilibrium_stencil_mesh.py and the runtime contract reserves shared Reckon state for the orchestrator.
follow_ons: Orchestrator writeback should replace or reconcile the eight old-name references in the six listed docs/state files after integration; none was writable by this node.
blockers: none
