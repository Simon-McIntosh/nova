node: hex-connectivity-partition
status: complete
commits: 8d4f3f33a67d61450d6081e4b4c240ae80db19a3
changed_paths: nova/equilibrium/flux_surface_connectivity.py; tests/test_flux_surface_connectivity.py; docs/figures/primary-xpoint-evidence/hex-connectivity-partition.json
tests: `UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest tests/test_flux_surface_connectivity.py` -> 11 passed in 34.29 s; final strengthened pinch assertion `uv run --no-sync pytest tests/test_flux_surface_connectivity.py::test_hex_neighbour_keeps_a_pinch_joined_that_four_neighbours_sever` -> 1 passed in 8.84 s; scoped Ruff check and format check passed; JSON parse and `git diff --check` passed
test_logs: /tmp/hex-connectivity-partition-tests-final.log; /tmp/hex-connectivity-partition-pinch-test.log; /tmp/hex-connectivity-partition-benchmark.log
artifacts: docs/figures/primary-xpoint-evidence/hex-connectivity-partition.json -> constructed 11x13 saddle pinch differs by 6 cells: four-connectivity gives 9 public and 6 private cells and assigns wall cell [6,3] PRIVATE/SHADOW, while six-neighbour hex connectivity gives 15 public and 0 private and assigns the same wall cell PUBLIC/ADMIT; same-process warm GPU benchmark at production 33x33 gives four-connectivity 3 iterations and 0.4911435 ms median versus six-neighbour connectivity 16 iterations and 0.766596 ms median, ratio 1.560839; isolated scalar arithmetic projection over 1,341,435 slices is 1,028.34 s total and 369.50 s incremental, not measured batched campaign throughput
evidence_inputs: The source now states the semantic mismatch directly: the scan labeller is a four-connected rectangular-lattice interpretation, while the shipped half-offset hexagonal plasma mesh uses centre-first six-neighbour rings shared by the null search and quadratic derivative operator; a diagonal hex neck at a saddle can therefore be severed only by the rectangular proxy and change the public/private wall classification. Both routes reuse one canonical minimum-label fixpoint driver; the hex route receives `hex_stencil`/tessellation ring rows and uses fixed-shape scatter-min propagation, remains JIT and vmap safe, closes a ring when its centre is unconfined, reserves zero for background, and returns minimum one-based flat-cell labels. The constructed pinch joins [4,5] to [5,4] through the physical (+1,-1) hex edge: rectangular labels are axis 32 and wall 69, hex labels are axis 32 and wall 32. Production behavior was not switched.
follow_ons: A separately authorised cutover decision should determine whether production classification consumes the six-neighbour labels; before campaign pricing, measure the kernel under actual production vmap batch widths because this node's catalog cost is a qualified scalar arithmetic projection.
blockers: none
