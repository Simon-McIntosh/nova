node: reachable-wall-limiter-point
status: blocked
commits: none
changed_paths: nova/equilibrium/connectivity_boundary.py; tests/test_connectivity_boundary.py
tests: `pytest -q tests/test_connectivity_boundary.py::test_reachable_wall_minimum_is_refined_along_polyline` — 1 passed in 15.82s; `pytest -q tests/test_connectivity_boundary.py::test_typed_saddle_selector_masks_points_outside_wall_polygon` — 1 passed after the reachable-limited fixture correction; required full connectivity and topology files not run because the cohort evidence retry fence stopped the node
test_logs: `.reckon-logs/reachable-wall-unit.log`; `.reckon-logs/reachable-wall-selector.log`; `.reckon-logs/reachable-wall-selector-fixed.log`; `.reckon-logs/reachable-wall-cohort.log`; `.reckon-logs/reachable-wall-cohort-fixed.log`
artifacts: none; `docs/figures/primary-xpoint-evidence/reachable-wall-limiter-point.json` was not written because the required 12-MAST-arm plus DIII-D measurement did not complete
evidence_inputs: Implemented a fixed-shape pre-saddle axis-component wall mask and cyclic along-wall quadratic minimizer; focused refinement evidence moved arc/coordinate by 0.25 m, reduced flux distance from 1.0625 to 1.0, and excluded a shadowed node at distance 0.1. The first banked measurement reconstructed the MAST cohort but failed at the DIII-D receipt's nested margin schema. The corrected rerun stopped on a MAST non-finite margin encoded as JSON null. No bank or existing figure was rewritten.
follow_ons: tried: two banked measurement adapters, with the first failing on missing top-level DIII-D `class_margin` and the second on `float(None)` for a MAST infinite margin; options: permit a third adapter correction that accepts null as positive infinity, derive old-margin non-finite state from the companion receipt field, or have the orchestrator recover the in-memory measurement with an existing schema-aware receipt reader; leaning: accept null as positive infinity and rerun once because that matches the frozen receipt encoding and preserves the exact 12+1 measure; cost-if-wrong: another full carrier reconstruction, about three minutes locally, plus re-audit of all row classifications
blockers: The same cohort measurement command failed twice after two different schema fixes, triggering the dispatch's mandatory stop condition before the 12+1 done-when, artifact, full test files, naming checks, and commit could be completed.
