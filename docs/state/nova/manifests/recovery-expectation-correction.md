node: recovery-expectation-correction
status: complete
commits: 95455769a505ddcb67f6ed44a8e4cf8188fc06d8
changed_paths: tests/test_two_class_recovery.py
tests: env -u VIRTUAL_ENV JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider -m 'slow or not slow' tests/test_two_class_recovery.py -> expected nonzero exit 1; 3 passed and 2 failed in 238.22 s, with no skips, errors, import failures, or collection failures; ruff check --fix and ruff format completed cleanly on the assigned file; git diff --check passed
test_logs: /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T102953707430-recovery-expectation-correction/test-two-class-recovery.log
artifacts: /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T102953707430-recovery-expectation-correction/test-two-class-recovery.log - exact gate outcome 3 passed and 2 failed; the two retained failures terminate after 10 Newton-Krylov steps at residual 1.29980506 for coarse and 1.30949372 for fine against the unchanged 1e-10 criterion
evidence_inputs: Three expectation corrections landed without rebanking or changing production code, iteration budgets, or acceptance magnitudes. The vmapped cold diverted branch is now asserted to achieve diverted class and remain topology-consistent while not converged. The perturbation ladder retains and exercises amplitudes 0.001, 0.01, and 0.05 but requires recovery only through the banked 0.01 relative amplitude; the 0.05 rung remains present and is not promoted to an established basin claim. Receipt semantics, classes, flags, counts, convergence, map results, source inputs, derived summaries, and the closure at exactly 4.440892098500626e-16 Wb remain exact; only primitive normalization-anchor coordinates and fluxes use an absolute roundoff envelope of one binary64 epsilon, 2.220446049250313e-16, with zero relative tolerance, admitting the measured one-epsilon axis-radius drift, 4.163336342344337e-17 m axis-height drift, 8.673617379884035e-19 Wb axis-flux drift, and 3.231430693775156e-19 Wb boundary-flux drift. Historical SHA-256 array digests remain required fields but are not compared for equality because they encode bitwise float64 identity rather than receipt semantics. A fixed ten-step local Newton-Krylov solve should not be expected to guarantee recovery from cold seeds 1.15254 and 1.13853 full root spans from the banked roots; those failures remain visible and unchanged.
follow_ons: Lead decision recommended: retain the ten-step and 1e-10 recovery gate as-is, and use topology-preserving continuation or a banked warm-start path when recovery from more than one full root span is a product requirement; alternatively define and measure a separate cold-start robustness budget before changing the existing test contract. The live plan HTML also contains authored sections after its closing html element; plan repair is outside this worker's write fence and should be audited by the orchestrator without mixing it into this test-only commit.
blockers: none
