NEEDS-HELP: The current solver reconstructs a zero-residual limited root whose state digest differs from the banked coexisting root, so fixture equivalence requires an authority decision before measurement.
node: two-branch-batch-cost
status: blocked
commits: 771a8a200e22dd6e830b4f8c7b3617f82c243897, 5e4eb3b7d156d28c1b5c49a61cfab0d9b9a37516
changed_paths: benchmarks/two_branch_batch_cost.py
tests: UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync ruff check benchmarks/two_branch_batch_cost.py — PASS; uv run --no-sync ruff format --check benchmarks/two_branch_batch_cost.py — PASS after formatting; git diff --check — PASS; SLURM job 1254529 — FAILED before timing because the current conditioned preparation did not converge (achieved limited, residual 2.650029e-01); diagnostic job 1254531 — PASS and showed conditioned residuals 2.088553e-01, 1.719516e-01, 1.273175e-01, and 8.863953e-02 at 30, 40, 60, and 100 steps; diagnostic job 1254536 — PASS and showed unconditioned preparation achieved limited with residual 0 at both 20 and 30 steps; SLURM job 1254538 — launcher failed before Python because uv was not installed inside the project environment; SLURM job 1254539 — FAILED before timing because the zero-residual limited root digest was 6f9defaedb131c8ad47192ef7e1b9d65f35975ba239189bd7cf0f20111a82774 rather than banked ea75c675928714add998bead5b31cc0e5317946c677186d344a96fe33e3507be.
test_logs: /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T085631015497-two-branch-batch-cost/two-branch-batch-cost.slurm.log; /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T085631015497-two-branch-batch-cost/limited-root-diagnostic.slurm.log; /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T085631015497-two-branch-batch-cost/limited-root-unconditioned.slurm.log; /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T085631015497-two-branch-batch-cost/two-branch-batch-cost-retry.slurm.log; /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T085631015497-two-branch-batch-cost/two-branch-batch-cost-retry-python.slurm.log
artifacts: docs/figures/dual-branch-selection/two-branch-batch-cost.json was not written; no valid timing rows exist.
evidence_inputs: tried: implemented the distinct-slice, converged-solve H200 benchmark with input read, host-to-device transfer, compile, warm-up, execute-only, convergence accounting, and campaign extrapolation separated; then attempted current root preparation and exact reconstruction of the banked root. options: (1) authorize the zero-residual, topology-consistent current limited root after an explicit physical-equivalence qualification and record its new digest; (2) recover and bank the exact historical state array matching ea75c675928714add998bead5b31cc0e5317946c677186d344a96fe33e3507be; (3) authorize rerunning the benchmark at the historical source revision that created the banked digest. leaning: option 1, because the current production operator reports the reconstructed state as limited with exactly zero relative residual and the timed current production solves would remain unchanged; qualify map residual, topology, flux span, and solution observables before accepting identity. cost-if-wrong: all H200 timing rows and the campaign-cost decision must be discarded and rerun against the recovered historical state or a newly approved fixture.
follow_ons: none outside scope; the orchestrator must choose the limited-root identity policy before redispatch.
blockers: The required H200 cost evidence cannot be produced without deciding whether a current zero-residual limited root with a different binary state digest is an acceptable replacement for the banked coexisting root. The same benchmark code path failed twice with different root-preparation fixes, so the node stopped as required.
