node: xpoint-spatial-evidence
status: complete
commits: 597417afae7432fcb60c59e90ddcd6375da8436e
changed_paths: benchmarks/diiid_forward_gs_match.py; docs/figures/plateau-input-attribution/margin-frame-remeasure.json; nova/equilibrium/connectivity_boundary.py; tests/test_connectivity_boundary.py; tests/test_jax_topology.py
tests: `env -u JAX_ENABLE_X64 JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider tests/test_connectivity_boundary.py::test_jit_vmap_grad_safe_and_fixed_shape` — 1 passed in 28.61 s in an isolated process with no inherited x64 environment; `JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider tests/test_jax_topology.py::test_batched_primary_points_match_per_slice` — 1 passed in 8.54 s; `JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider tests/test_connectivity_boundary.py::test_forward_topology_margin_matches_boolean_transition_and_terminal_gate` — 1 passed in 27.53 s; `JAX_PLATFORMS=cpu UV_PROJECT_ENVIRONMENT=/home/ITER/mcintos/Code/nova/.venv PYTHONPATH="$PWD" uv run --no-sync pytest -p no:cacheprovider tests/test_connectivity_boundary.py tests/test_jax_topology.py` — 22 passed in 142.57 s; path-scoped `ruff check` — clean; regeneration command `python -m benchmarks.diiid_forward_gs_match --margin-frame-remeasure` — EXIT=0, internal sign-parity check 53/53 with zero disagreements.
test_logs: /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T075958475442-xpoint-spatial-evidence/focused-topology-suite.log; /home/ITER/mcintos/.config/reckon/crew/runs/r-20260825T075958475442-xpoint-spatial-evidence/margin-remeasure-regeneration.log
artifacts: `docs/figures/plateau-input-attribution/margin-frame-remeasure.json` — 10/10 terminal records now carry selected X coordinate, raw flux, normalized X operand, typed-candidate table, connectivity-local admission diagnostics, and wall operand before/after shadow rejection; all 462 pre-existing numeric leaves are exactly unchanged, while the fresh replay compared 447 non-duration numeric leaves exactly with zero differences and preserved 15 banked duration fields; exact typed-saddle counts are 1–3 per terminal, wall operands are admitted on 10/10, and connectivity-local admission retains 0 candidates on 10/10; double-null parity fixture — 2 finite X candidates, selected coordinate [1.1019047226795156, -0.4448939832318258] m, selected flux 2.6776133229622467 Wb, axis-to-X span 3.1072837439676566 Wb, two wall/X near-tangency slices at ±3.107283743967657e-10 Wb.
evidence_inputs: Measure 1 holds: `Topology.read_batch` primary-X coordinates match per-slice reads to 8 machine eps and X flux matches exactly on three slices spanning a two-saddle double-null field and both sides of a near-tangent wall/X flux tie; the test no longer proves only the O-point. Measure 2 holds: the banked ten-terminal DIII-D receipt was regenerated in place and enriched additively; every old field, including `terminal_class_margin` and aggregate distributions, is value-identical, with 462/462 existing numeric leaves unchanged. The receipt distinguishes `no_typed_saddle_candidate`, `typed_saddle_operand_nonfinite`, `selected_typed_saddle_not_connectivity_reachable`, and `selected_typed_saddle_with_connectivity_support`, while the wall record separately distinguishes absent, private-flux-shadow-rejected, and admitted extrema and records both pre-shadow and admitted operands. On this ten-terminal DIII-D bank all exact X operands are finite, 1–3 typed saddles exist per terminal, all wall operands are admitted, and no connectivity-local candidate survives the flood-rejoin/binding-flux admission; the preserved class margins remain -0.5295734642179688 to -0.1783497398371257 and all terminals remain limited. Measure 3 holds: `test_jit_vmap_grad_safe_and_fixed_shape` now calls `configure_dtypes()` itself and passed alone after explicitly removing `JAX_ENABLE_X64` from the process environment. The regeneration did not cover frozen MAST references 21978/35 or 22086/43, so no claim about their +infinity operand cause is made here.
follow_ons: The frozen-six MAST receipt egress is outside this node's fence; its 21978/35 and 22086/43 absent operands can reuse `traced_margin_candidate_diagnostics` to separate no typed saddle from reachability or wall-shadow rejection, but those two references were not remeasured in this DIII-D bank regeneration.
blockers: none
