NEEDS-HELP: the reserved H200 allocation starts, but the GPU job step is rejected before measurement because this Slurm installation forbids combining its inherited task TRES with `--gpus-per-task`.

tried: The first reserved launch requested eight CPUs and pended with scheduler reason `Resources`; reducing the request to the seven free group-A CPUs started the job. A 96-surface-bin CPU probe then established that the accuracy configuration is too expensive for the 55-minute multi-lane allocation: explicit lowering and compilation took 7.038763228 s, while one post-compile 129x129 service execution took 607.032261677 s. The harness was therefore set to the service default of 28 surface bins, with every expensive execution counted in its median, and reordered to secure the GPU, historical and host receipts before CPU service measurements. The first revised job failed before metrics with its output stranded on compute-local `/tmp`; moving the batch log to the durable crew-run directory exposed the repeat failure exactly: `srun: fatal: gpus-per-task is mutually exclusive with tres-per-task`. Job 1252994 held one H200 under reservation `gpu_0003_grpA`, used `TMPDIR=/tmp`, requested seven CPUs, and failed after 10 s. The TSV records that unavailable lane rather than silently skipping it.

options: (1) Remove `--gpus-per-task=1` from the inner GPU `srun`; the enclosing allocation already requests exactly one `h200` GPU and contains one task. (2) Run the GPU command directly in the batch shell, which already owns the single task and GPU, retaining `JAX_PLATFORMS=cuda`. (3) Replace the shorthand with this site's explicit GPU TRES syntax after checking the local Slurm contract, while avoiding a second per-task TRES declaration.

leaning: Option 1. It is the smallest correction and preserves `srun --cpu-bind=cores`; the allocation already proves the requested H200 and reservation were granted, so another GPU request at the step boundary is redundant.

cost-if-wrong: A wrong step-level GPU binding costs another approximately 10-second pre-measurement failure and no scientific artifact. If the step starts but does not inherit the allocation's GPU visibility, the H200 lane must be marked unavailable with the exact backend message and the launcher needs option 2 or 3; none of the CPU, retired-kernel or host-contour numbers can be treated as the requested comparative result until the full companion TSV is produced in one declared configuration.

## Banked harness

`measure.py` checks that TORAX's shipped `iterhybrid_cocos17.eqdsk` is the real 129x129 map, builds the current extraction service at 24 radial cells and its declared default 28 surface bins, and measures single-map and eight-map `jax.vmap` executables. It disables the persistent compilation cache, records lowering-plus-compilation separately, then states the repetition count and median total and per-map execution wall time. Eight input maps have distinct flux scales, with axis and boundary anchors scaled consistently.

The historical lane imports the complete `nova/` tree from a `git archive c51c09fc~1` overlay at commit `a0798fe28a342dd8fb922ef1dae2ddc917db4e3c`; it never switches this worktree. The host route calls `FluxSurfaceGeometry.from_flux_map` on the same EQDSK with 129 surfaces and 256 angles, both for one map and an explicitly labelled eight-call Python loop. CPU processes pin `JAX_PLATFORMS=cpu`; GPU processes pin `JAX_PLATFORMS=cuda`; every lane runs in a fresh process so the known long-lived-process XLA accumulation cannot contaminate cross-route timing.

No requested benchmark verdict is claimed. `results.tsv` contains only the schema and the unavailable H200 launch receipt; the 96-bin CPU probe above is a configuration-cost diagnostic, not the final 28-bin benchmark.
