# Faithful moment integration receipt

This receipt re-evaluates the independent closed-form oracle state after the
fixed degree-fifteen Duffy moment route becomes the sole production path. The
analytic state is evaluated directly at every solve target. Its exterior supply
is constructed from an independently integrated analytic-density plasma image,
so the production map cannot reproduce the state tautologically.

`results.json` places the faithful-route forcing beside the banked degree-nine
fit baseline and records topology-excluded moment suprema. The associated tests
keep the stored DINA reproduction lane separate: its current-error pins combine
the stored reference inconsistency with the adopted representation, whereas the
closed-form forcing receipt isolates representation fidelity.
